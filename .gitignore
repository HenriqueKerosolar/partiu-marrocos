node_modules/
.pnpm-store/

# Next.js
.next/
out/
build/
dist/

# env
.env
.env.local
.env.*.local

# Prisma
packages/db/prisma/*.db

# Postgres local (alternativa ao Docker, ver scripts/local-postgres.mjs)
.pgdata/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# editor
.vscode/*
!.vscode/extensions.json
.idea/

# test/coverage
coverage/

# TypeScript incremental build info
*.tsbuildinfo

# Claude Code — estado de sessão local, nunca versionado (launch.json é dev
# config útil pro time, fica fora desta lista de propósito)
.claude/scheduled_tasks.lock

# Site público — WIP explicitamente marcado "não publicar" pelo próprio
# nome da pasta; nunca deveria entrar no histórico do repositório (nem no
# site publicado, nem no controle de versão).
site-original/_nao-publicar/
